# AI Platform Architecture Notes

This directory contains design notes for RAG, semantic search, natural language query, and prompt-driven self-service experiences.

- `rag/` = retrieval augmented generation pipelines
- `semantic_layer/` = embeddings and query abstractions
- `nl_query/` = natural language interface adapters
- `prompt_templates/` = reusable prompt patterns
